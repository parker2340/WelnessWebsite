@model Exception

<h2>An error occurred</h2>

<p>
    Sorry, an error occurred while processing your request.
</p>

@if (Model != null)
{
    <h3>Error details:</h3>
    <pre>@Model.ToString()</pre>
}